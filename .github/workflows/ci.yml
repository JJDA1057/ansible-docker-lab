name: Test Workflow

on: 
    push:

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - name: show files
              
              run: ls -la

            - name: Show the current directory
            
              run: pwd